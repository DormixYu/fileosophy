# Fileosophy 重构实施计划

> 基于 PRD-v1.md | 预估：8-10 个阶段，每阶段 1-3 小时

---

## 阶段 0：清理 + 基础设施准备

### 任务
1. 删除共享功能代码：
   - 删除 `src/pages/SharingPage.tsx`
   - 删除 `src/components/sharing/*.tsx`（6 个文件）
   - 删除 `src/stores/useShareStore.ts`（或相关 store）
   - 从路由（App.tsx）中移除 `/sharing` 路由
   - 从 Layout 侧边栏中移除"共享"导航项
   - 清理 tauri-api.ts 中的 shareApi（可保留接口，标记 deprecated）
2. 重写 CSS 设计系统（index.css）：
   - 全新扁平化色彩系统
   - 全新动效系统
   - 全新组件基础样式
3. 更新 tailwind.config.js

### 交付物
- 共享功能代码已清理
- 全新 CSS 设计系统就绪
- 构建通过

---

## 阶段 1：首页重构（Dashboard）

### 任务
1. 重写 DashboardPage.tsx：
   - 顶部全局搜索栏
   - 板块网格（进行中/规划中/已暂停/本月完成/全部/归档库）
   - 每个板块显示项目数量，点击跳转到筛选后的项目列表
   - 底部双栏：标记文件汇总 + 待办事项汇总
2. 新增板块统计逻辑（从 useProjectStore 获取数据）
3. 页面入场动画（板块 stagger、搜索栏 fade-in）

### 交付物
- 全新首页可用
- 板块点击可跳转到项目列表（带筛选参数）
- 构建通过

---

## 阶段 2：项目模板 + 新建项目重构

### 任务
1. 新增模板数据模型（AppSettings 中存储模板列表）
2. 实现默认模板（投标/项目/行政/学习/其他）
3. 重写 ProjectDialog：
   - 第一步：选择模板（卡片式选择，显示模板名+图标）
   - 第二步：输入项目名称（自动生成编号）
   - 确认 → 自动创建项目文件夹 + 项目记录
4. 设置中新增模板管理页面
5. 新建项目动效（模态框弹出 + 模板卡片 hover 效果）

### 交付物
- 新建项目 2 步完成
- 模板可自定义
- 项目文件夹自动创建
- 构建通过

---

## 阶段 3：项目列表重构

### 任务
1. 重写 ProjectListPage.tsx：
   - 表格视图 + 卡片视图双模式切换
   - 接收首页传入的筛选参数（状态、分类）
   - 筛选器栏：分类、状态、日期范围
   - 搜索栏
   - 批量操作栏
2. 新建项目按钮 → 打开新的模板化对话框
3. 项目行/卡片动效（stagger 入场、hover 上浮）
4. 空状态设计

### 交付物
- 全新项目列表页
- 双视图切换
- 筛选排序正常
- 构建通过

---

## 阶段 4：项目详情页重构 + 文件管理增强

### 任务
1. 重写 ProjectDetailPage.tsx：
   - 顶部信息栏（简洁）
   - 标签页：文件管理 / 看板 / 甘特图
2. 重写文件管理区域：
   - ⭐ 标记文件置顶区
   - 文件夹树
   - 文件列表（名称、大小、修改时间）
   - 文件标记/备注功能（Star + Note）
   - QuickLook 预览（空格键触发）
3. 新增 FileBookmark 数据模型和 API
4. 文件操作动效（列表 stagger、标记星星动画）

### 交付物
- 文件标记/备注可用
- 文件预览可用（至少文本/图片/PDF）
- 构建通过

---

## 阶段 5：看板重构 + 文件关联

### 任务
1. 重写看板组件：
   - 现代化卡片设计
   - 卡片关联文件（选择项目内文件）
   - 卡片关联文件夹
   - 待办完成动效
2. 新增 CardFileLink 数据模型
3. 看板卡片动效（拖拽、入场、完成）

### 交付物
- 看板卡片可关联文件/文件夹
- 点击关联文件可直接打开
- 构建通过

---

## 阶段 6：归档系统

### 任务
1. Rust 端新增归档 API：
   - `archive_project` — 压缩项目文件夹为 .zip
   - `unarchive_project` — 解压到原路径
   - `get_archived_projects` — 获取归档列表
2. 前端归档库页面
3. 项目列表中增加"归档"操作
4. 归档/解归档动效

### 交付物
- 归档/解归档可用
- 归档库页面可浏览搜索
- 构建通过

---

## 阶段 7：分类自定义 + 设置增强

### 任务
1. 设置中新增分类管理（CRUD）
2. 设置增强：
   - 主题切换（暗色/浅色/跟随系统）
   - 字体大小调节
   - 项目根路径设置
   - 默认模板选择
   - 快捷键自定义
3. 全局搜索增强（搜备注、标签）

### 交付物
- 分类可自定义
- 设置页完整
- 构建通过

---

## 阶段 8：系统状态栏图标 + 工作会话

### 任务
1. macOS 状态栏图标（Tauri 插件）
2. 状态栏 Popover 菜单：
   - 快速搜索
   - 标记文件
   - 待办列表
   - 保存/恢复工作会话
   - 新建项目
3. 工作会话记忆：
   - 检测当前打开的文件/应用/网页
   - 保存到 WorkSession 模型
   - 一键恢复
4. WorkSession 数据模型 + API

### 交付物
- 状态栏图标可用
- 工作会话保存/恢复可用
- 构建通过

---

## 阶段 9：全局审查 + 测试 + 打磨

### 任务
1. 全局 UI 审查（一致性检查）
2. 暗色模式完整测试
3. 动效统一检查
4. 构建 + 打包测试
5. 清理无用代码

### 交付物
- 最终版本可用
- 无 TypeScript 错误
- 所有功能正常
